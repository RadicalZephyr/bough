[package]
name = "bough"
description = ""
authors = ["Zefira Shannon"]
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
tokio = { version = "1.0", features = ["rt-multi-thread", "macros"] }
